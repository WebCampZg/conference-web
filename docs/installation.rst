============
Installation
============

At the command line::

    $ easy_install conference-web

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv conference-web
    $ pip install conference-web
